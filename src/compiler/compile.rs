use crate::{compiler::Heap, types::Term};

impl Heap {
    pub fn compile(&mut self, expr: Term) {
        use Term::*;
        // match expr {
        //     Question(_) => todo!(),
        //     _ => todo!(),
        // }
    }
}
